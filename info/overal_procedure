cloud setup:
drone, onion, supervisor, -> talk to gabriel server


local device:

maps, kml, hermes -> py, commander, send the py to the cloudlet.


running:
download the scrpt, put the tasks of the scrpt into the task queue, supervisor receive the task command, flight the drone
